name: Deploy

on:
  push:
    branches: [main]

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Start server with dummy keys
        run: |
          ANTHROPIC_API_KEY=dummy \
          ELEVENLABS_API_KEY=dummy \
          node server.js &
          echo $! > server.pid
          sleep 2

      - name: Health check
        run: |
          curl -sf http://localhost:3000/health | grep '"ok":true'

      - name: Stop server
        if: always()
        run: kill $(cat server.pid) || true

  deploy:
    needs: smoke-test
    runs-on: ubuntu-latest
    env:
      HAS_RENDER_WEBHOOK: ${{ secrets.RENDER_DEPLOY_WEBHOOK != '' }}
      HAS_DEPLOYED_URL: ${{ secrets.DEPLOYED_URL != '' }}
    steps:
      - name: Trigger Render deploy
        if: env.HAS_RENDER_WEBHOOK == 'true'
        run: |
          curl -sf -X POST "${{ secrets.RENDER_DEPLOY_WEBHOOK }}"

      - name: Wait for deploy to settle
        if: env.HAS_RENDER_WEBHOOK == 'true'
        run: sleep 60

      - name: Verify deployed health
        if: env.HAS_DEPLOYED_URL == 'true'
        run: |
          curl -sf "${{ secrets.DEPLOYED_URL }}/health" | grep '"ok":true'
